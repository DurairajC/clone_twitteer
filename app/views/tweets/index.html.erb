<section class="section">
  <div class="container">
    <div class="columns">
      
       <%= render 'feed' %>
       
       <% if user_signed_in? %>
       <%= render 'who_to_follow' %>
       <% end %>
    </div>
  </div>
</section>

